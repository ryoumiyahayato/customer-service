# Copy this file to .env.local for local development or to .env on your server.
# Production must use a long random secret, for example: openssl rand -hex 32
AUTH_SECRET=change-this-to-a-long-random-secret
PORT=3000
DATA_DIR=./data
DATABASE_PATH=./data/support.sqlite
NODE_ENV=production
