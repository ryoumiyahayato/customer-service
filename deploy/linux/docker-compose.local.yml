services:
  app:
    ports:
      - "127.0.0.1:${LOCAL_APP_PORT:-8788}:${APP_PORT:-3000}"
    environment:
      APP_DOMAIN: ${APP_DOMAIN:-127.0.0.1}
      VISITOR_ROOT_DOMAIN: ${VISITOR_ROOT_DOMAIN:-127.0.0.1}
      APP_BIND_HOST: 0.0.0.0
