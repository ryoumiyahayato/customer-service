plugins {
    id "com.android.application"
    id "org.jetbrains.kotlin.android"
}

android {
    namespace "net.customerchat.app"
    compileSdk 35

    defaultConfig {
        applicationId "net.customerchat.app"
        minSdk 26
        targetSdk 35
        versionCode 1
        versionName "0.1.0"
    }
}
