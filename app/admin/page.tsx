'use client';
import { useEffect, useMemo, useState } from 'react';

type AdminUser = { id: string; username: string; role: 'SUPER_ADMIN' | 'OPERATOR'; is_disabled?: number };
type View = 'sessions' | 'operators' | 'staffChat';
const groups = [{ key: 'PENDING', label: '待接入' }, { key: 'OPEN', label: '处理中' }, { key: 'CLOSED', label: '已关闭' }, { key: 'ARCHIVED', label: '已归档' }, { key: 'DELETED', label: '删除暂存' }];

export default function Admin() {
  const [admin, setAdmin] = useState<AdminUser | null>();
  const [view, setView] = useState<View>('sessions');
  const [sessionsOpen, setSessionsOpen] = useState(true);
  const [operatorsOpen, setOperatorsOpen] = useState(false);
  const [sessions, setSessions] = useState<any[]>([]);
  const [operators, setOperators] = useState<any[]>([]);
  const [cur, setCur] = useState<any>();
  const [msgs, setMsgs] = useState<any[]>([]);
  const [text, setText] = useState('');
  const [quote, setQuote] = useState<any>(null);
  const [staffMsgs, setStaffMsgs] = useState<any[]>([]);
  const [staffText, setStaffText] = useState('');
  const [openGroups, setOpenGroups] = useState<Record<string, boolean>>({ PENDING: true, OPEN: true, CLOSED: false, ARCHIVED: false, DELETED: true });
  const [notice, setNotice] = useState('');
  const isSuper = admin?.role === 'SUPER_ADMIN';

  function disabledExit() { alert('该账户权限已被禁用'); location.href = '/'; }
  async function checkMe() { const r = await fetch('/api/auth/me'); const d = await r.json().catch(() => ({})); if (d.disabled) disabledExit(); setAdmin(d.admin || null); return d.admin; }
  async function load() { const r = await fetch('/api/sessions?includeDeleted=1'); if (r.ok) setSessions((await r.json()).sessions || []); }
  async function loadOperators() { const r = await fetch('/api/admins/operators'); if (r.ok) setOperators((await r.json()).operators || []); }
  async function loadStaff() { const r = await fetch('/api/staff-chat'); if (r.status === 403) disabledExit(); if (r.ok) setStaffMsgs((await r.json()).messages || []); }
  useEffect(() => { checkMe().then(a => { if (a) { load(); loadStaff(); if (a.role === 'SUPER_ADMIN') loadOperators(); } }); }, []);
  useEffect(() => { if (!admin) return; const timer = window.setInterval(() => { checkMe(); load(); loadStaff(); if (isSuper) loadOperators(); if (cur && !cur.deleted_at) open(cur, false); }, 4000); return () => window.clearInterval(timer); }, [admin?.id, cur?.id, cur?.deleted_at, isSuper]);
  useEffect(() => { if (!admin || typeof EventSource === 'undefined') return; const es = new EventSource('/api/events'); es.onmessage = () => { load(); if (view === 'staffChat') loadStaff(); if (isSuper) loadOperators(); }; es.onerror = () => es.close(); return () => es.close(); }, [admin?.id, view, isSuper]);

  async function logout() { await fetch('/api/auth/logout', { method: 'POST' }); location.href = '/'; }
  async function updateProfile(e: any) { e.preventDefault(); const fd = new FormData(e.currentTarget); const username = String(fd.get('username') || '').trim(); const password = String(fd.get('password') || ''); const r = await fetch('/api/admins/profile', { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ username, password }) }); const d = await r.json().catch(() => ({})); if (r.ok) { setNotice('管理员资料已修改，请重新登录'); await logout(); } else setNotice(d.error || '修改失败'); }
  async function createOperator(e: any) { e.preventDefault(); const fd = new FormData(e.currentTarget); const r = await fetch('/api/admins', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ username: fd.get('username'), password: fd.get('password') }) }); const d = await r.json().catch(() => ({})); if (r.ok) { e.currentTarget.reset(); setNotice('一般客服账号已创建'); await loadOperators(); } else setNotice(d.error || '创建失败'); }
  async function disableOp(op: any) { if (!confirm(`确认禁用客服账号 ${op.username}？`)) return; const r = await fetch('/api/admins/operators', { method: 'DELETE', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ id: op.id }) }); const d = await r.json().catch(() => ({})); setNotice(r.ok ? '客服账号已禁用，相关会话已进入删除暂存' : (d.error || '操作失败')); await loadOperators(); await load(); }
  async function hardDeleteOp(op: any) { if (!confirm(`彻底删除 ${op.username} 并释放名称？`)) return; const r = await fetch('/api/admins/operators', { method: 'DELETE', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ id: op.id, hard: true }) }); const d = await r.json().catch(() => ({})); setNotice(r.ok ? '客服账号已彻底删除，名称已释放' : (d.error || '操作失败')); await loadOperators(); }
  async function open(s: any, markRead = true) { setCur(s); setQuote(null); if (s.deleted_at) { setMsgs([]); return; } if (markRead) await fetch(`/api/sessions/${s.id}/read`, { method: 'POST' }); const r = await fetch('/api/visitor', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ visitorId: s.visitor_key }) }); const d = await r.json(); setMsgs(d.messages || []); }
  async function act(path: string) { const r = await fetch(path, { method: 'POST' }); if (r.status === 403) disabledExit(); await load(); if (cur) await open(cur, false); }
  async function deleteSession(s: any) { if (!confirm('删除后 1 天内可以撤回，超过 1 天会自动清除。确认删除？')) return; await fetch(`/api/sessions/${s.id}/delete`, { method: 'POST' }); setCur(undefined); setMsgs([]); await load(); }
  async function restore(s: any) { const r = await fetch(`/api/sessions/${s.id}/restore`, { method: 'POST' }); const d = await r.json().catch(() => ({})); setNotice(r.ok ? '会话已恢复' : (d.error || '恢复失败')); await load(); }
  async function send(imagePath?: string) { if (!cur || cur.deleted_at || (!text.trim() && !imagePath)) return; const r = await fetch('/api/messages', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ sessionId: cur.id, senderType: 'OPERATOR', content: imagePath ? '' : text, messageType: imagePath ? 'image' : 'text', imagePath, quoteMessageId: quote?.id }) }); if (r.status === 403) disabledExit(); setText(''); setQuote(null); await open(cur, false); }
  async function upload(e: any) { const f = e.target.files?.[0]; if (!f) return; const fd = new FormData(); fd.append('file', f); const r = await fetch('/api/upload', { method: 'POST', body: fd }); const d = await r.json(); if (d.path) await send(d.path); else setNotice(d.error || '上传失败'); e.target.value = ''; }
  async function recall(m: any) { const r = await fetch(`/api/messages/${m.id}/recall`, { method: 'POST' }); const d = await r.json().catch(() => ({})); setNotice(r.ok ? '消息已撤回' : (d.error || '撤回失败')); if (cur) await open(cur, false); }
  async function purgeImages() { if (!confirm('清除本账号已发送或已接收会话里的所有图片内容？')) return; await fetch('/api/messages/purge-images', { method: 'POST' }); setNotice('图片内容已清除'); if (cur) await open(cur, false); }
  async function sendStaff(e: any) { e.preventDefault(); if (!staffText.trim()) return; const r = await fetch('/api/staff-chat', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ content: staffText }) }); if (r.status === 403) disabledExit(); if (r.ok) { setStaffText(''); await loadStaff(); } }
  function quoteText(id: string) { const m = msgs.find(x => x.id === id); if (!m) return '引用的消息'; if (m.status === 'recalled') return '消息已撤回'; return m.message_type === 'image' ? '[图片]' : m.content; }

  const grouped = useMemo(() => Object.fromEntries(groups.map(g => [g.key, sessions.filter(s => g.key === 'DELETED' ? s.deleted_at : (!s.deleted_at && s.status === g.key))])), [sessions]);
  if (admin === undefined) return <div className="page"><div className="login"><h2>正在加载</h2></div></div>;
  if (!admin) return <div className="page"><div className="login"><h2>无账户</h2><button onClick={() => { location.href = '/'; }}>返回主页</button></div></div>;

  return <div className="admin"><aside className="side"><div className="brand"><h2>客服工作台</h2><span>{isSuper ? '最高管理员' : '一般客服'}</span></div><nav className="side-nav"><button className={view === 'sessions' ? 'active' : ''} onClick={() => { setView('sessions'); setSessionsOpen(x => !x); }}>访客会话</button>{sessionsOpen && view === 'sessions' ? groups.map(g => <section className="folder" key={g.key}><button className="folder-head" onClick={() => setOpenGroups(x => ({ ...x, [g.key]: !x[g.key] }))}><span>{openGroups[g.key] ? '▾' : '▸'} {g.label}</span><b>{grouped[g.key].length}</b></button>{openGroups[g.key] ? <div className="folder-body">{grouped[g.key].map((s: any) => <button className={'session ' + (cur?.id === s.id ? 'active' : '')} onClick={() => open(s)} key={s.id}><b>{s.display_name}</b>{s.unread_count > 0 ? <span className="badge">{s.unread_count}</span> : null}<p>{s.deleted_at ? `已删除 ${new Date(s.deleted_at).toLocaleString()}` : (s.operator_name || '未接入')}</p></button>)}</div> : null}</section>) : null}{isSuper ? <button className={view === 'operators' ? 'active' : ''} onClick={() => { setView('operators'); setOperatorsOpen(x => !x); }}>客服账户</button> : null}{isSuper && operatorsOpen && view === 'operators' ? <div className="folder-body small-list">{operators.map(op => <button key={op.id} className="session"><b>{op.username}</b><p>{op.is_disabled ? '已禁用' : op.online ? '在线' : '离线'}</p></button>)}</div> : null}<button className={view === 'staffChat' ? 'active' : ''} onClick={() => setView('staffChat')}>客服内部联系</button></nav></aside><main className="main"><div className="toolbar"><div><b>{view === 'operators' ? '客服账户管理' : view === 'staffChat' ? '客服内部联系' : (cur?.display_name || '请选择会话')}</b><small>{admin.username} · {admin.role}</small></div><div className="toolbar-actions"><button className="btn secondary" onClick={logout}>退出到前台</button>{view === 'sessions' ? <button onClick={purgeImages}>清除我的图片</button> : null}{view === 'sessions' && cur && !cur.deleted_at ? <><button onClick={() => act(`/api/sessions/${cur.id}/assign`)}>接入</button><button className="btn secondary" onClick={() => act(`/api/sessions/${cur.id}/close`)}>关闭</button><button className="btn danger" onClick={() => deleteSession(cur)}>删除</button></> : null}{view === 'sessions' && cur?.deleted_at ? <button onClick={() => restore(cur)}>撤回删除</button> : null}</div></div>{notice ? <div className="notice">{notice}</div> : null}{view === 'sessions' ? <div className="workspace"><section className="chat-panel"><div className="msgs">{msgs.map(m => <div key={m.id} className={'msg ' + (m.sender_type === 'OPERATOR' ? 'me' : '')}>{m.quote_message_id ? <div className="quote-box">{quoteText(m.quote_message_id)}</div> : null}{m.status === 'recalled' ? <span>消息已撤回</span> : m.message_type === 'image' && m.image_path ? <img src={m.image_path} alt="聊天图片" /> : <span>{m.content || '[图片已清除]'}</span>}<div className="time">{new Date(m.created_at).toLocaleString()} {m.sender_type === 'OPERATOR' ? (m.is_read ? '已读' : '未读') : ''}</div><div className="msg-actions"><button onClick={() => setQuote(m)}>引用</button>{m.sender_type === 'OPERATOR' && m.sender_id === admin.id && m.status !== 'recalled' ? <button onClick={() => recall(m)}>撤回</button> : null}</div></div>)}</div>{cur && !cur.deleted_at ? <div className="composer">{quote ? <div className="quote-compose">引用：{quote.status === 'recalled' ? '消息已撤回' : quote.message_type === 'image' ? '[图片]' : quote.content}<button onClick={() => setQuote(null)}>取消</button></div> : null}<label className="file-btn">图片<input type="file" accept="image/jpeg,image/png,image/webp" onChange={upload} /></label><input type="text" value={text} onChange={e => setText(e.target.value)} onKeyDown={e => { if (e.key === 'Enter') send(); }} placeholder="输入回复" /><button onClick={() => send()}>回复</button></div> : <div className="empty-state">{cur?.deleted_at ? '该会话在删除暂存中，1 天内可以撤回。' : '从左侧选择一个访客会话'}</div>}</section></div> : null}{view === 'operators' && isSuper ? <div className="workspace"><aside className="admin-panel wide"><h3>修改最高管理员</h3><form onSubmit={updateProfile} className="mini-form"><input name="username" placeholder="新管理员名称，留空则不修改" autoComplete="off" /><input name="password" type="password" placeholder="新密码，至少 10 位，留空则不修改" autoComplete="new-password" /><button>保存并重新登录</button></form><h3 className="panel-title">创建一般客服</h3><form onSubmit={createOperator} className="mini-form"><input name="username" placeholder="一般客服账号" required autoComplete="off" /><input name="password" type="password" placeholder="初始密码，至少 8 位" required autoComplete="new-password" /><button>创建一般客服</button></form><h3 className="panel-title">客服账户</h3><div className="operator-list">{operators.length ? operators.map(op => <div className="operator-row" key={op.id}><div><b>{op.username}</b><span>{op.is_disabled ? '已禁用' : op.online ? '在线' : '离线'}{op.last_seen_at ? ` · ${new Date(op.last_seen_at).toLocaleString()}` : ''}</span></div>{op.is_disabled ? <button className="btn danger" onClick={() => hardDeleteOp(op)}>彻底删除释放名称</button> : <button className="btn danger" onClick={() => disableOp(op)}>禁用客服</button>}</div>) : <div className="empty-state">无账户</div>}</div></aside></div> : null}{view === 'staffChat' ? <div className="workspace"><section className="chat-panel"><div className="msgs">{staffMsgs.map(m => <div key={m.id} className={'msg ' + (m.sender_admin_id === admin.id ? 'me' : '')}><b>{m.sender_name}</b><div>{m.content}</div><div className="time">{new Date(m.created_at).toLocaleString()}</div></div>)}</div><form className="composer staff-composer" onSubmit={sendStaff}><input type="text" value={staffText} onChange={e => setStaffText(e.target.value)} /><button>发送</button></form></section></div> : null}</main></div>;
}
