# Frontend module

The runnable Next.js pages live in `/app`. This module documents the frontend boundary: UI code renders pages and calls `/api/*`; it must not contain database access, authorization decisions, or business workflow mutations.
